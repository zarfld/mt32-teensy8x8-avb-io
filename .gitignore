# KiCad
*.kicad_prl
*.kicad_dbl
fp-info-cache
_autosave-*
*.bak
*.orig
*.sch-bak
*.kicad_pcb-bak
gerbers/
fabrication/

# KiCad netlist and symbol cache
*.net
*.xml
# Note: sym-lib-table and fp-lib-table are project-local KiCad library tables.
# They should normally be committed for reproducibility so that anyone cloning
# the repo can open the project and find the correct libraries.
# Only ignore these files if you are certain they contain machine-specific paths
# that must not be shared.
# sym-lib-table
# fp-lib-table

# Build outputs
build/
dist/
out/
output/
*.o
*.elf
*.bin
*.hex
*.map

# Python virtual environments and caches
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Node (if any tooling)
node_modules/

# Logs
*.log
*.tmp

# OS / editor files
.DS_Store
Thumbs.db
.directory
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-workspace

# Temporary files
*.tmp
*.temp
/tmp/

# Submodule lock artefacts (submodule directories themselves are tracked)
# (intentionally empty — submodule directories are versioned via .gitmodules)
